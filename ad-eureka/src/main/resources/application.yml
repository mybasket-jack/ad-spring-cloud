spring:
  application:
    name: ad-eureka
server:
  port: 8000
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: false # 是否从eureka 获取配置
    register-with-eureka: false
    service-url:
      defaultZone: http://${euraka.instance.hostname}:${server.port}/eurka/
